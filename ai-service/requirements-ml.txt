# Optional heavy research dependencies.
# Install these when running real multilingual embeddings, CrossEncoder reranking,
# or training the Transformer section-quality classifier.
sentence-transformers==3.3.1
transformers==4.47.1
torch
